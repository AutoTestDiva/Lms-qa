name: Java CI with Gradle (JDK 15, Gradle 8.3)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest
#    permissions:
#      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 15
        uses: actions/setup-java@v3
        with:
          java-version: '15'
          distribution: 'adopt'

  # !  Installation Node.js
      - name: ✅ ⚙️ Install Node version "20"
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: ❎ ⌛ Check Node version
        run: node --version

  # !  List of folders before tests
      - name: ✅ 📂 List Directories before tests
        if: always()
        run: ls -R

#      - name: Stop Gradle Daemon
#        run: ./gradlew --stop

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.3'

      - name: ✅ ⚙️ Set execute permission for gradlew
        run: chmod +x gradlew

      - name: Get application.yml from secrets
        run: |
          echo "${{ secrets.APPLICATION_YML }}" > src/test/resources/application.yml

  # !  List of folders before tests
      - name: ✅ 📂 List Directories before tests
        if: always()
        run: ls -R


## убирает точку перед src
#      - name: List Directories before tests
#        if: always()
#        run: |
#          find src/test/resources/DataScv -type f

#      - name: Build with Gradle Wrapper
#        run: ./gradlew build

#      - name: Run tests
#        run: |
#          ./gradlew clean qa -Psuite3

#      - name: Modify CSV file paths
#        run: |
#            sed -i 's/src\/test/.src\/test/' src/test/java/de/aittr/lms/CSVDataProviders.java
      
      

      - name: Run tests
        run: |
          run: npm lms-qa1 --suite=1
#         ./gradlew clean qa -Psuite2
#         ./gradlew clean qa -Psuite3
#         ./gradlew clean qa -Psuite4

      - name: Deploy
        run: echo 'Deploying...'
